import { Component, computed, signal } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { ProductCardComponent } from '../shared/product-card.component'; import { PRODUCTS,CATEGORIES } from '../core/mock-data';
@Component({standalone:true,imports:[FormsModule,ProductCardComponent],template:`<div class="container page"><div class="crumb">Home / Shop all</div><div class="listing-head"><div><h1>Discover something exceptional</h1><p>Curated pieces for a more considered everyday.</p></div><button class="filter-mobile">☷ Filters</button></div><div class="listing"><aside><h3>Filters <button (click)="category.set('')">Clear all</button></h3><label>Category</label>@for(c of categories.slice(0,7);track c.name){<button class="filter-item" [class.active]="category()===c.name" (click)="category.set(c.name)"><span>{{c.name}}</span><small>{{c.count}}</small></button>}<label>Price range</label><input type="range" min="0" max="600" [ngModel]="maxPrice()" (ngModelChange)="maxPrice.set($event)"><div class="price-range"><span>$0</span><span>{{maxPrice()}}</span></div><label>Rating</label><button class="filter-item">★★★★☆ <small>& up</small></button><label>Availability</label><button class="filter-item">In stock <small>882</small></button></aside><div class="results"><div class="results-bar"><b>{{filtered().length}} products</b><div><button>▦</button><button>☰</button><select [ngModel]="sort()" (ngModelChange)="sort.set($event)"><option value="popular">Most popular</option><option value="low">Price: Low to high</option><option value="high">Price: High to low</option><option value="rating">Top rated</option></select></div></div><div class="product-grid listing-grid">@for(p of filtered().slice(0,12);track p.id){<app-product-card [product]="p"/>}</div><div class="pagination"><button>‹</button><button class="current">1</button><button>2</button><button>3</button><span>…</span><button>9</button><button>›</button></div></div></div></div>`}) export class ProductsComponent {categories=CATEGORIES;category=signal('');maxPrice=signal(600);sort=signal('popular'); filtered=computed(()=>PRODUCTS.filter(p=>(!this.category()||p.category===this.category())&&p.price<=this.maxPrice()).sort((a,b)=>this.sort()==='low'?a.price-b.price:this.sort()==='high'?b.price-a.price:this.sort()==='rating'?b.rating-a.rating:b.reviews-a.reviews))}
